<% if logged_in? %> 
<% @customer = current_user %>
<%= render partial: 'partial/cut', locals: { haircut: @customer.haircuts.build } %> 
<% else %>
<%= render partial: 'partial/cut', locals: {haircut: @haircut = Haircut.new} %> 
<% end %>